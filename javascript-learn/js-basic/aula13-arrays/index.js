const alunos = [`Joca`, `Beta`, `Pepo`] //Array
console.log(alunos)

alunos.push(`Tony`) //Add a element in the final of the array
console.log(alunos)

alunos.unshift(`Mars`)  //Add a element at the start of the array
console.log(alunos)

const removed = alunos.pop() //Delete the final element of the array
console.log(alunos)

const removed2 = alunos.shift() //Delete the initial element of the array
console.log(alunos)
